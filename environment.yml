name: workshop
channels:
  - pytorch
  - defaults
  - conda-forge
  - bioconda
dependencies:
  - python=3.7.0
  - cython
  - ipython
  - matplotlib
  - numpy
  - pandas=1.3.0
  - pytorch=1.6.0
  - pip
  - pip:
      - biopython
      - fire
      - memoization
      - regex


